<form method="post" action="{{ path('perform_delete', {'id': perform.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this perform?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perform.id) }}">
    <div class="btn-save">
        <button class="btn"><i class="far fa-trash-alt"></i> Delete</button>
    </div>
</form>
