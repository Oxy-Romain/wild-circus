{% extends 'admin/layout_admin.html.twig' %}

{% block title %}Edit Member{% endblock %}

{% block body %}
    <div class="adjust-format">
        <div class="form-admin">
            <div class="card-form">

                <h1>Edit Member</h1>

                {{ include('admin/member/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('admin_index') }}" class="btn-back-list">
                    <button class="btn btn-outline-dark"><i class="fas fa-arrow-left"></i> Back to list</button>
                </a>

                {{ include('admin/member/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
