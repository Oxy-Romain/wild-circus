<!--Navbar-->
<nav class="navbar navbar-expand-lg fixed-top">
    <!-- Navbar brand -->
    <a class="navbar-brand" href="{{ path('home_index') }}">Wild Circus</a>
    <!-- Collapse button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#basicExampleNav"
            aria-controls="basicExampleNav" aria-expanded="false" aria-label="Toggle navigation">
        <i class="fas fa-hamburger"></i>
    </button>
    <!-- Collapsible content -->
    <div class="collapse navbar-collapse" id="basicExampleNav">
        <!-- Links -->
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home_index') }}#performPart">PERFORM</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home_index') }}#crewPart">THE CREW</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home_index') }}#pricePart">PRICE</a>
            </li>
            {% if app.user %}
            <li class="nav-item log">
                <a class="nav-link" href="{{ path('admin_index') }}">ADMIN VIEW</a>
            </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav log">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link nav-user" href="#">{{ app.user.username }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">LOG OUT</a>
                </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">LOG IN</a>
                    </li>
            {% endif %}
        </ul>
        <!-- Links -->
    </div>
    <!-- Collapsible content -->
</nav>
<!--/.Navbar-->
